{
  "name": "cypressframework",
  "version": "1.0.0",
  "description": "cypressProject",
  "main": "index.js",
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": false,
    "cucumberJson": {
      "generate": true,
      "outputFolder": "cypress/cucumber-json",
      "filePrefix": "",
      "fileSuffix": ".cucumber"
    }
  },
  "scripts": {
    "test": "test",
    "cypress:open": "cypress open",
    "parallel": "cypress-parallel -s cy:run -t 2 -d \\cypress\\integration\\-a '\"-- b chrome --headed"
  },
  "author": "Andrei",
  "license": "ISC",
  "dependencies": {
    "cypress": "^8.6.0",
    "parallel": "^1.2.0"
  },
  "devDependencies": {
    "cypress-audit": "^1.1.0",
    "cypress-cucumber-preprocessor": "^4.3.0",
    "multiple-cucumber-html-reporter": "^1.18.0"
  }
}
